/* eslint-env jasmine */
(function () {
  'use strict';
  describe('Retrieving images', () => {
    //let app;
    beforeEach(() => {
    });
    it("should pass a jasmine config sanity check", () => {
      expect(2+3).toBe(5);
    });
  });
})();
